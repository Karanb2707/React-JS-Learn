CSS 2:

gradients:
gradients let you display smooth transitions between 2 or more colors
1. linear gradients : goes down/up/left/right 
2. radial gradients : defined by there center
3. conic gradients : rounded around center point

shadows:
give shadow to ur text: text-shadow: horizontal-len vertical-len blur color (4 parameters)
we can give multiple shadow to same text
box shadow: default color of shadow is color of text

overflow:
overflow: auto -> use to fit and scroll the content in div properly

css postiton property
static, relative, fixed, absolute, sticky

transform 2D:
translate(v,h), rotate(deg), scale(v,h), skew(deg)